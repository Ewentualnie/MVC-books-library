export type Book = {
    year: number;
    name: string;
    description: string;
    preview: string;
    title: string;
    pages: string;
    authors: string
}

export type newBook ={
    year: number;
    name: string;
    description: string;
    preview: string;
    title: string;
    pages: string;
}